{
  "private": true,
  "scripts": {
    "test": "yarn workspace tests test",
    "fix": "prettier --write '**/*.{js,js.hbs,json,json.hbs}'"
  },
  "workspaces": [
    "modules/*"
  ],
  "devDependencies": {
    "np": "^5.0.3",
    "prettier": "1.18.2"
  }
}
