{
  "private": true,
  "scripts": {
    "test": "yarn workspace tests test",
    "fix": "prettier --write '**/*.{js,js.hbs,json,json.hbs}'"
  },
  "workspaces": [
    "modules/*",
    "modules/tests/integration/sandbox/*",
    "modules/tests/integration/sandbox/*/e2e-tests"
  ],
  "devDependencies": {
    "prettier": "1.18.2"
  }
}
